// Eval for GID-270 (model-place), part B: /domain/model is the canonical
// place — nothing is judged here.
package model

// User — the canonical place for a data struct.
type User struct {
	ID    string
	Email string
}
