// Eval for GID-193: the CamelCase word-boundary case for the suffix — package story.
package story

// History — the package name story is only the tail of the word "History",
// not a separate CamelCase word: no stutter.
type History struct{}
