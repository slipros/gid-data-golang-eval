// Positive: the same rule for /domain/service.
package redis // want `GID-138: package "svc/domain/service/redis"\. Fix: grouping subpackages in /domain/service are forbidden, keep layer entities at its root`

type Cache struct{}
