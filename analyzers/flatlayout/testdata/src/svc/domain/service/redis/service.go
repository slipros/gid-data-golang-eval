// Позитив: то же правило для /domain/service.
package redis // want `GID-138: пакет "svc/domain/service/redis" — группирующие подпакеты в /domain/service запрещены, сущности слоя живут в его корне`

type Cache struct{}
